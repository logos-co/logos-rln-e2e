# register — the module-stack e2e: fund the node's own payer, RLN membership
# registration, merkle proof + registry cross-check, proof
# generation/verification (valid + tampered).
NODES=1
NEEDS_MODULES="liblogos_lez_rln_module liblogos_rln_module"
TARGETS="local testnet"
RUNNER=bash
