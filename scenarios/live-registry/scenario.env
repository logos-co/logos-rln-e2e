# live-registry — the registry-provider crate's gated live-chain tests
# (testnet_*), driven against whatever deployment the target provisioned.
# No daemon: cargo test against the chain.
NODES=0
NEEDS_MODULES=""
TARGETS="local testnet"
RUNNER=bash
