# mix — 5-node mixnet with per-hop RLN spam protection (logos-delivery mix
# stack), membership pre-provisioned on-chain (STATUS.md).
NODES=5
NEEDS_MODULES="logos_execution_zone liblogos_lez_rln_module"
TARGETS="local"
RUNNER=bash
