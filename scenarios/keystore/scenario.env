# keystore — the module keystore's lifecycle acceptance for the delivery
# integration: locked-store surfaces, restart persistence (real verifier
# unlock, idempotent re-register, message_id continuity), the exclusive
# keystore lock (held / contended / recovered), slot-per-proof accounting,
# quota-for-absent-membership, and quarantine surfacing on the wire.
# Registers once (faucet-paid), then probes; needs E2E_FUNDING=faucet.
NODES=1
NEEDS_MODULES="lez_core liblogos_lez_rln_module liblogos_rln_module"
TARGETS="local testnet"
RUNNER=bash
