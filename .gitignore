# nix build outputs
result
result-*

# per-run harness scratch
runs/

# released CLI tarballs the operator-path scenario caches (see harness/lib/usertools.sh)
.cache/

# local scratch
*.log

# stale build artifact from the pre-rework layout
docker/lp2p-out/

# local working doc (kept untracked)
scenarios/mix/JOURNEY.md
.gcroots/
